def f(x,y,z,a=10,b=20):
    """
    Возвращает сумму пяти переменных с тремя обязательными
    и двумя необязательными
    :параметры x, y, z, a, b: целые числа
    :return: целое число.
    """
    return x + y + z + a + b
print(f(1,2,3))
